apiVersion : apps/v1
kind: Deployment
metadata:
  name: "wordpresskub-e28d"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "wordpresskub-e28d"
  template:
    metadata:
      labels:
        app: "wordpresskub-e28d"
    spec:
      containers:
        - name: "wordpresskub-e28d"
          image: "containerizedsiteacr.azurecr.io/wordpresskub"
          ports:
          - containerPort: 80
apiVersion: v1
kind: Service
metadata:
    name: "wordpresskub-e28d"
    labels:
        app: "wordpresskub-e28d"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "wordpresskub-e28d"